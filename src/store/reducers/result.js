const initialState = {
    wordsPerMinutes: 60,
    frappes: '',
    correctWords: '',
    incorrectWords: ''
};

const reducer = (state = initialState, action) => {
    return state;
};

export default reducer;